<h1>Movie Lists</h1>

<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>

<%= link_to "Add a New List", new_list_path, class: 'btn btn-primary' %>
<%# create a movie list button %>
<%# create a link to new list %>
